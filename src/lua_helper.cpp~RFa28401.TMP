#include "lua_helper.h"
#include "obs.h"

void lua_print_error(lua_State* state)
{
	const char* message = lua_tostring(state, -1);
	blog(LOG_DEBUG, message);
	lua_pop(state, 1);
}

const luaL_Reg helperlib[] = {
	{ NULL, NULL }
};

void lua_open_helper(lua_State* L)
{
	luaL_newlib(L, helperlib);
}